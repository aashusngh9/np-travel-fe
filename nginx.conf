server {
    listen 80;
    server_name farecompare.site www.farecompare.site;

    # Redirect HTTP to HTTPS
    return 301 https://farecompare.site$request_uri;
}

server {
    listen 443 ssl;
    server_name www.farecompare.site;

    ssl_certificate /etc/letsencrypt/live/farecompare.site/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/farecompare.site/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    return 301 https://farecompare.site$request_uri;
}

server {
    listen 443 ssl;
    server_name farecompare.site;

    ssl_certificate /etc/letsencrypt/live/farecompare.site/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/farecompare.site/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        try_files $uri /index.html;
    }

    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }
}
